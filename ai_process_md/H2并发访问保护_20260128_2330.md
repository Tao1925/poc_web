# H2 并发访问保护

## 目标
- 避免同一个 H2 文件被多个进程直接同时打开

## 变更
- 新增启动锁：应用启动时为 H2 文件创建 `*.process.lock` 并持有文件锁
- 导出脚本默认使用备份 zip 进行恢复导出，避免直连运行中数据库
- README 增加并发访问注意事项

## 说明
- 如需强制直连，导出脚本需加 `--allow-live-db` 且保证服务已停止
- 备份路径默认 `data/backups`
