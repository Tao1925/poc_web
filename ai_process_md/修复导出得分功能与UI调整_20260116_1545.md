# 修复导出得分功能与UI调整

## 变更时间
2026-01-16 15:45

## 变更内容

### 1. 前端功能修复
- **文件**: `src/main/resources/templates/grading.html`
- **问题**: 用户反馈点击“导出得分数据”无反应。
- **原因**: 经检查，`grading.html` 中虽然添加了按钮，但对应的 JavaScript 函数 `exportScores` 未被正确包含在 `<script>` 标签内。
- **修复**: 补全了 `exportScores` 函数的 JavaScript 代码，实现了 CSV 文件的下载和保存逻辑。

### 2. UI 布局调整
- **文件**: `src/main/resources/templates/grading.html`
- **需求**: 将“导出得分数据”按钮调整至“导出答题数据”按钮上方。
- **修改**:
  - 修改 `.export-action` 样式类，增加 `display: flex; flex-direction: column; gap: 10px;`，使按钮垂直排列。
  - 调整 HTML 结构中按钮的顺序（代码中已是正确顺序，配合 CSS 即可生效）。
  - 移除按钮的内联 `margin-right` 样式。

### 3. 后端逻辑确认
- **文件**: `src/main/java/com/example/quiz/controller/GradingController.java`
- **状态**: 确认 `exportScores` 接口逻辑正确，无需修改。
